{
  "name": "base",
  "version": "0.0.1",
  "description": "Base electron/TypeScript project.",
  "main": "js/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepublish:comment": "Prepublish gets run on `npm install`",
    "prepublish": "npm run build",
    "clean": "rm -rf js",
    "clean_all": "npm run clean && rm -rf node_modules",
    "start": "electron ."
  },
  "keywords": [
    "electron",
    "typescript"
  ],
  "author": "Cody Casterline",
  "license": "MIT",
  "devDependencies": {
    "@types/electron": "^1.3.17",
    "@types/node": "^6.0.33",
    "electron-prebuilt": "^1.3.3",
    "typescript": "~2.0"
  }
}
